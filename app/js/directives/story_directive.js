angular.module('app').directive('userstory', function () {
    var linker = function (scope, element, attrs) {

    };
    var controller = function ($scope) {

    };
    return { restrict: 'A', controller: controller, link: linker };
});